{
  "name": "urlshortner_ejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
"build": "echo Building project... && rm -rf dist && mkdir dist && cp -r View dist/View && [ -d public ] && cp -r public dist/public || true && cp Main.js dist/ && cp Connection.js dist/ && [ -d Routes ] && cp -r Routes dist/Routes || true && [ -d Model ] && cp -r Model dist/Model || true",
  "dev": "nodemon Main.js",
  "test": "echo \"No tests specified\" && exit 0"
},
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "mongoose": "^8.7.1",
    "nodemon": "^3.1.7",
    "shortid": "^2.2.16"
  }
}
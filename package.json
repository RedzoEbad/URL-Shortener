{
  "name": "urlshortner_ejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "build": "echo Building project... && rm -rf dist && mkdir dist && cp -r View dist/views && [ -d public ] && cp -r public dist/public || true && cp Main.js dist/ && cp Connection.js dist/ && cp -r routes dist/routes && cp -r models dist/models",
  "start": "node dist/Main.js",
  "dev": "nodemon Main.js",
  "test": "echo \"No tests specified\" && exit 0"
},
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "mongoose": "^8.7.1",
    "nodemon": "^3.1.7",
    "shortid": "^2.2.16"
  }
}